# /etc/cron.d/heat-during-low-tariff-crontab 

SHELL=/bin/sh
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

# m h dom mon dow user	command
0  11	* * *	root    cd /opt/camera && ./camera-capture.sh
#
